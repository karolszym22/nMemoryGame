class Player
{
    constructor(trials)
    {
        this.trials = trials;
    }

    trialsCount = () => this.trials++;
    
    sendToLocalStorage(player)
    {
      const {nick, results} = player;
    }

        
    
}